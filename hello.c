/* Print a message to stdout in the default 'text' mode.
 * On Windows this results in a CR LF line ending. */

#include <stdio.h>

int main()
{
    printf("Hello, World!\n");
    return 0;
}
